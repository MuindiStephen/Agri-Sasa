package com.ekenya.rnd.merchant.ui.fragments.select_contact

import android.app.Application
import androidx.lifecycle.AndroidViewModel

import javax.inject.Inject

class ViewModelSelectContacts @Inject constructor(application: Application) : AndroidViewModel(application) {

    companion object {
        private const val TAG = "SelectContactsViewModel"
    }

}

