version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run:
          name: "Shamba App"
          command: "echo Shamba App"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
